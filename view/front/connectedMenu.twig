<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom fixed-top">
    <a class="navbar-brand" href="{{ BASE_URL }}">
      <figure>
        <img src="{{ BASE_URL }}public/images/logo.png" alt="">
        <figcaption>Epic Friends</figcaption>
      </figure>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav m-auto">
        <li class="nav-item">
          <a class="nav-link mx-2" href="{{ BASE_URL }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link mx-2" href="{{ BASE_URL }}search-engine">Rechercher un Epic Friend</a>
        </li>
        {% if  user.userTypeId() == 5 %}
        <li class="nav-item">
            <a class="nav-link mx-2" href="{{ BASE_URL }}mailbox">Messagerie</a>
        </li>
        {% endif %}
      </ul>
      <div>
            <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
                <li class="nav-item d-flex align-items-center m-2">
                    <p id="hello-text" class="navbar-text m-0">Bonjour {{ user.username() }} !<p>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link" href="" id="dropdown-menu-perso" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {% if user.userTypeId() == 3 or user.userTypeId() == 4 %}
                        <img src="https://img.icons8.com/color/64/000000/administrative-tools.png">
                        {% endif %}
                        {% if  user.userTypeId() == 5 %}
                        <img class="profile-picture border" src="{{ BASE_URL }}public\images\profiles-pictures\{{ user.profilePicture() }} ?>" alt="">
                        {% endif %}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdown-menu-perso">
                        {% if user.userTypeId() == 3 or user.userTypeId() == 4 %}
                        <a class="dropdown-item" href="{{ BASE_URL }}users-management">Gérer les membres</a>
                        <a class="dropdown-item" href="{{ BASE_URL }}interests-management">Gérer les centres d'intérêts</a>
                        {% endif %}
                        {% if  user.userTypeId() == 5 %}
                        <a class="dropdown-item" href="{{ BASE_URL }}my-profile">Mon profil</a>
                        {% endif %}
                        <a class="dropdown-item" href="{{BASE_URL}}my-account">Mon compte</a>
                        <a class="dropdown-item" href="{{BASE_URL}}disconnect">Me déconnecter</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
  </nav>
  
  